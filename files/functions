# Git functions
current_branch() {
    echo `git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/'`
}

# Ruby functions
maybe_zeus() {
    if [ -f '.zeus.sock' ]
    then
        zeus "$@"
    else
        `"$@"`
    fi
}
